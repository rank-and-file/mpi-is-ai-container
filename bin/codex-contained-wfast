#!/bin/sh
CONTAINER="/fast/${USER}/containers/ai-tools.sif"
BINDS="--bind $PWD --bind $HOME/.codex --bind /fast/${USER}"
exec apptainer exec --nv --writable-tmpfs --contain $BINDS --pwd "$PWD" "$CONTAINER" codex --yolo "$@"
