#!/bin/sh
CONTAINER="/fast/${USER}/containers/ai-tools.sif"
BINDS="--bind $PWD --bind $HOME/.gemini --bind /fast/${USER}"
exec apptainer exec --nv --writable-tmpfs --contain $BINDS --pwd "$PWD" "$CONTAINER" gemini --yolo "$@"
